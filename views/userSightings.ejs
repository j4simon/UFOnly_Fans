<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Encounters</title>
</head>
<body>
    <a href="/sightings/create">Log a new encounter</a>
    <h1>Sightings List</h1>
    <ul class="sightings">
    <% User.forEach((el) => { %>
      <a href="/sightings/<%= el._id %>">
        <li><%= el.title %> occured on <%= el.date %> at <%= el.location %></li>
      </a>
    <% }) %>
    </ul>    
</body>
</html>